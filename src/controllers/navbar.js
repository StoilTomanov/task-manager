export function showNavBar() {
    const navBar = document.getElementById('navbar');
    const openBar = document.getElementById('toggleOpen');
    const closeBar = document.getElementById('toggleClose');
}